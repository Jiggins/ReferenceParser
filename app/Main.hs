module Main where

import Text.Parsec

import Parser

-- parseFromFile p fname = do
--   input <- readFile fname
--   return (runParser p () fname input)
--
--
-- main :: IO ()
-- main = parseFromFile reference "input.txt" >>= print

main = return ()
